# Open Questions

- When should the `feature/onboarding-ui` app be moved from `apps/` into `apps/web-app/` to match `pnpm-workspace.yaml` and `MONOREPO_GUIDE.md`?
- Which issue should own frontend TanStack Query hooks and response guards that consume `@linko/contracts`?
- Should future codegen enable Hey API SDK/TanStack Query plugins once the frontend API layer is ready?
