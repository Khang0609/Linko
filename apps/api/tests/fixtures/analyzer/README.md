# Analyzer Mock Evaluation Fixture

This is a deterministic mock plumbing fixture.
It is not the canonical Golden Set and must not be used
to claim model accuracy.

The two cases only verify evaluator wiring with `MockProvider`. Live Vertex
quality, latency, and human-reviewed Golden Set acceptance remain external to
this repository fixture.
